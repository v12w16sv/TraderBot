# Binance API Credentials (can be dummy values in mock mode)
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# Set to true for mock mode (no real API calls)
MOCK_MODE=true

# Use testnet (ignored in mock mode)
TESTNET=true